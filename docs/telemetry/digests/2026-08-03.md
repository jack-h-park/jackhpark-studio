## Weekly Takeaways

- **Satisfaction is unmeasurable this week.** Zero 👍/👎 ratings were collected across the window, so there is no satisfaction signal at all — far below the ~5-rating floor needed to trust a rate. The blocker is feedback adoption, not sentiment: with only 1 chat request recorded in PostHog, there was essentially no traffic to rate.
- **Traffic is effectively zero, so every rate below is a 1–2 sample.** 2 traces scored / 5 scores total, 1 PostHog chat request, 4 Langfuse traces. Treat all percentages in this digest as anecdotes, not trends.
- **Retrieval looks bad on the two knowledge traces we have.** Insufficient rate 100% (2/2), and highest-similarity averaged 0.18 with a min of 0.00 and max of 0.37 — both traces landed well under any usable relevance bar, and one retrieved nothing similar at all. Worth pulling those two traces in Langfuse by hand before drawing a conclusion about ranking; n=2 is too small to justify a topK or weight change on its own.
- **The single errored/aborted chat request was local dev traffic, not production.** *(Corrected after follow-up investigation — see note below.)* The one `chat_completion` event in this window is `env=dev` (2026-07-31, `upstream_error`, ~30s timeout), so the 100% error / 100% abort rates describe a dev request. The digest's HogQL (`scripts/telemetry/weekly-digest.ts:187`) filters only on event name and timestamp, with no `env` predicate, so local dev traffic is counted in the "Product metrics (PostHog)" section alongside production.
- **The one real production chat this week succeeded — and is missing from PostHog.** Langfuse holds a `prod` trace at 2026-08-01T22:15:43Z (`aborted: false`, retrieval used, gpt-4o-mini) with no corresponding `chat_completion` event in PostHog. Root cause identified the same day: the chat client sends no `x-user-id` / `x-anonymous-id` / `x-request-id` header and no body `sessionId`, so `resolvePosthogDistinctId()` returns null and capture is skipped silently for real visitors. **PostHog chat volume is therefore not a trustworthy traffic count until visitor identity is wired** — use the Langfuse trace count instead.
- **Proxy vs. human divergence could not be computed.** No trace carried both a `user_feedback` and a `retrieval_insufficient` score, so both diagnostic cells ("insufficient but 👍", "sufficient but 👎") are empty. Until 👍/👎 collection produces any volume, the divergence table — the point of the digest — stays blind. The feedback path was checked (see note below) and is deployed and healthy; the constraint is opportunity count, not a broken widget.

> **Follow-up note (feedback-widget check, 2026-08-03).** The 👍/👎 path was verified after this digest ran: the widget ships in the production bundle, `/api/chat-feedback` is live in production (405 on GET, 400 on a body with no `traceId`), and the server-side emit path has a working end-to-end score in Langfuse. Langfuse holds only 3 `user_feedback` scores ever, all dev verification runs (2026-06-11 ×2, 2026-07-17 ×1) — zero organic ratings. With ~1 completed production answer in this window, the absence of ratings is explained by traffic, not breakage. The one path not verifiable without live production traffic is whether the `X-Trace-Id` response header reaches a real browser; the widget renders only when that header is present and the answer completed without error.

---

# Weekly telemetry digest

**Window:** 2026-07-27 → 2026-08-03
**Volume:** 2 traces scored, 5 scores total

## Product metrics (PostHog)

| Metric | Value |
| --- | --- |
| Chat requests | 1 |
| Distinct users | 1 |
| Latency p50 / p95 / p99 | 30.0s / 30.0s / 30.0s |
| Error rate | 100% |
| Abort rate | 100% |
| Response cache hit rate | 0% |
| Avg tokens / request | — |

## Engineering metrics (Langfuse)

| Metric | Value |
| --- | --- |
| Traces | 4 |
| Observations | 25 |
| Total model cost | — |
| Cost / trace | — |
| Generation latency p95 | 29.9s |

## User satisfaction (👍/👎)

| Metric | Value |
| --- | --- |
| Responses rated | 0 |
| 👍 up | 0 |
| 👎 down | 0 |
| Satisfaction rate | — |

## Retrieval quality

| Metric | Value |
| --- | --- |
| Highest similarity (avg) | 0.18 (min 0.00, max 0.37, n=2) |
| Insufficient rate | 100% (2/2) |
| Unique docs in context (avg) | 6.0 (n=1) |

## Proxy vs. human (divergence)

No traces carried both a feedback and an insufficient score this period, so proxy/human agreement can't be measured yet.

## Takeaways

- Retrieval insufficient on 100% of knowledge traces — consider raising topK or revisiting ranking weights.
- No user feedback collected this period — 👍/👎 adoption is zero.
